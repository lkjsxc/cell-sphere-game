# Evolution Ability Regions v1

Status: terminal implementation record. Exact remote, CI, Pages, deployed-byte,
and deployed-browser proof is carried by the final exact-revision handoff because
the pushed commit is itself the publication subject. A failed or absent external
postcondition makes this package active at that exact condition.

## Starting point

- Branch `main` at `222e2e88c46169daf55c163f3dc861b89fdea28a`, tracking
  `origin/main` with ahead/behind `0/0`.
- The exact starting revision had successful verify run `33322605664` and Pages
  deployment `6168921482`. The public GitHub API reported no open issue or pull
  request.
- `docs/work/README.md` named no active package. The cell-progression and
  World-substrate packages are terminal predecessor evidence, so this is the
  one active package.
- `AGENTS.md` began modified with the user-supplied replacement contract and is
  campaign-owned input.
- Untracked user transfer notes `docs/work/202608300500.md`,
  `docs/work/202608300855.md`, `docs/work/202608301243.md`,
  `docs/work/202608302226.md`, and `docs/work/202608311044.md` remain preserved
  byte-for-byte and unstaged.

## Confirmed root causes

- The predecessor layout fixed the root at cell `0`, assigned near-equal quotas
  with a `1_000_000_000` same-neighbor penalty, and accepted a maximum component
  size of `8` plus at least `95%` diverse closed neighborhoods.
- Fixed-substrate cell `0` is Deep Ocean with growth suitability
  `0.014999999664723873`, nutrient `0.3361009359359741`, moisture
  `0.6499999761581421`, and zero green-land neighbors.
- Predecessor digest `db40b2ed` had isolated non-root occurrences, largest
  archetype component `1`, one-hop diversity `1`, and domain component counts
  Foundation `243`, Fertility `157`, Freshwater `234`, Scarcity `238`,
  Cryogenic `189`, Marine `219`, and Luminous `238`.
- The renderer truthfully projected that authoritative scatter. Stronger tint
  could not correct the map/root authority mismatch.

## Selected decisions and deviations

- Retain topology level `4`, all `2,562` exact progression cells and `7,680`
  direct edges, 42 archetypes, one-root/62–63 quota law, cost law, production
  compiler, substrate seed `0xe701c311`, and the maintained World field owner.
- Cache the topology and fixed substrate in pure owners. Layout version `2`
  consumes both once. It ranks the green root exactly as mandated, selects
  substrate/tier/separation-aware domain seeds, reserves a compact exact
  suitability-led Fertility frontier, initializes one compact simultaneous
  spherical power partition, closes exact capacities with bounded connected
  topology-frontier transfers, then subdivides each domain into connected exact
  archetype capacities. Stable cell ties and a hard visit budget replace random
  retry or backtracking.
- Fertility's protected frontier is deliberate: the first connected candidate
  passed the numerical gates by only `0.000582` while occupying `70.6%` water.
  The selected final region is `97.3%` land, `42.2%` green biome, growth
  suitability `0.775401`, and has suitability margin `+0.277519`.
- A hexagonal winning root has six neighbors, but the predecessor catalog had
  only five non-root tier-1 Foundation archetypes. `Stable Transport` changes
  presentation tier `2 → 1` to supply the sixth truthful root-ring identity;
  cost, role, effects, and compiler inputs are unchanged.
- The connected Foundation launch basin made the retained tier-5 `World Shaper`
  cells pull the global tier-5 median inside tier 4. `World Shaper` joins the
  existing tier-6 finale (`5 → 6`) so the mandated tier 1–5 medians are strict;
  its cost, role, effects, and compiler inputs are unchanged.
- One immutable two-bit archetype/domain relation is packed above the existing
  three-bit dynamic state in the shared edge byte. Dynamic selected, recent,
  frontier, and owned state dominates quiet region structure in both existing
  renderer paths. No pass or per-frame partition work was added.
- Progression/layout/content identities advance together. Predecessor Evolution
  levels, transaction receipts, Imprints, and Evolution History reset; unrelated
  validated meta, World History, and Trophy History remain under their existing
  independent validators. No cell migration table exists.
- The old five-World specialist-ownership tournament gate encoded scattered
  adjacency. It is retained as an observation, not a pass gate. Production
  agents now prove every specialist through a production-derived direct path,
  bounded public candidates, and a real exact transaction; natural campaigns
  must still make route progress.

## Completed coherent phases

- Reconciled branch/upstream/worktree, predecessor packages, current owners,
  tests, CI/Pages orientation, and the supplied root contract.
- Captured the scattered baseline in production audits and Chrome 152 across
  Worker/WebGL2, fallback/WebGL2, and fallback/Canvas 2D.
- Cut over root, layout, identity, persistence, WAL/transaction, History,
  Imprint, agent, audit, and semantic fixture consumers as one authority.
  Anti-adjacency scoring, sparse-layout acceptance gates, root-0 assumptions,
  and current scattered-layout claims are deleted.
- Added shared region-edge semantics to the existing WebGL boundary draw and
  Canvas boundary phase. Canvas clips limb-crossing segments and distinguishes
  domain structure without hiding the fixed geography.
- Reconciled current README, status, decision, design, rendering,
  accessibility, testing, performance, source-owner, audit, and browser docs.

## Final local identities and layout facts

- Progression `8`; level vector `3`; layout `2`; content `10`; content hash
  `7fd92cf7`; Imprint `3`; Evolution History `3`; agent observation `9`.
- Root cell `2265`, biome enum `5` (Grass), land, six of six direct green-land
  neighbors, growth `1.059999942779541`, nutrient `0.7472620010375977`,
  moisture `0.6074897646903992`, and temperature `0.4741906225681305`.
- Layout digest `09da2261`; edge digest `c03988ac`; repeat construction is byte
  identical. First Division occurs once. Archetypes `1–19` (`Reliable Budding`
  through `Shore Rooting`) occur `63` times each; archetypes `20–41` (`Deep
  Reserve` through `Living Biosphere`) occur `62` times each. All 42 archetype
  component counts are `1`.
- Domain capacities/components are Foundation `627/1`, Fertility `187/1`,
  Freshwater `376/1`, Scarcity `374/1`, Cryogenic `250/1`, Marine `312/1`, and
  Luminous `436/1`.
- Tier 1–5 root-distance medians are `8, 20, 24, 29, 31.5`. The root ring is six
  distinct tier-1 Foundation archetypes.
- Own-suitability margins are Foundation `+0.099318`, Fertility `+0.277519`,
  Freshwater `+0.048520`, Scarcity `+0.012444`, Cryogenic `+0.364971`, Marine
  `+0.337126`, and Luminous `+0.050110`. Marine water fraction is `1.0` versus
  `0.415556` outside Marine; Freshwater influence is `0.196637` versus
  `0.126721` global; Cryogenic temperature is `0.124716` versus `0.478455`;
  Scarcity growth is `0.251457` versus `0.344229`; Fertility green fraction is
  `0.422460` versus `0.154567` and growth is `0.775401` versus `0.344229`.
- Edge relation counts are `6,148` internal, `985` archetype perimeter, and
  `547` domain perimeter.

## Focused verification

- PASS — 57/57 focused layout, transaction, compiler-equivalence, persistence,
  WAL, History, Imprint, renderer projection, agent, and active-World tests.
- PASS — `npm run audit:skills`: repeat digest `09da2261`, construction
  `164.344 ms` in the final complete run, `7,048,903` accounted visits under
  `80,000,000`, exact full traversal, legal representative routes `11–31` hops,
  and bounded huge-rank compilation. Report `reports/skill-audit.json` is
  `11,696` bytes, SHA-256
  `e11404eddb6ea1ee172e2e7e2d2d2dadff39d1101cad01d66a968e145aa24642`.
- PASS — `npm run agent:smoke`: 10/10 agent tests, 12 deterministic production
  campaigns, no task failures, bounded traces, and six legal real-transaction
  specialist routes of `15, 15, 15, 15, 17, 26` hops with `37–64` candidates
  under limit `224`. Report is `11,274` bytes, SHA-256
  `fe2571e5e4025acfe0d7509d74d69d434b130ae65d92e12679b097708793b766`.
- PASS — `npm run balance:smoke`: eight paired production Worlds per fresh,
  Foundation, Scarcity, Luminous, Marine, and mature fixture; all terminal and
  finite, no immortality, Luminous powered `8/8`, Marine median lifetime
  `194.2 s`, mature median `216.4 s`. Report is `92,421` bytes, SHA-256
  `e581727f3a82be579e7e8ce8021457548f4804785e9109ed3733627b80878c66`.
- PASS — `npm run benchmark`: authoritative hashes remain `15863d52` and
  `e32ad0ff`, terminal tick `1492`, measured `12,361 ticks/s` with deterministic
  fixed trace and all finite profiles.
- PASS — `npm run audit:luminous`: real charge gates, decay, terminal clearing,
  renderer hierarchy, and deterministic finite Worlds remain valid.
- PASS — `npm run audit:trophies`: the production 240-World calibration remains
  deterministic (`3428e54a`), earns `22` Trophies at World 48 and `28` at World
  240, advances through `24` exact Evolution cells, preserves idempotent awards,
  and has no impossible or duplicate criterion. Its predecessor long-horizon
  bands assumed scattered neighbors exposed cheap first ranks and are
  superseded; no cost, effect, reward, or Trophy criterion changed. Report
  SHA-256 is `140a26f9522491cff1024e295cc81ba50b82fa2500ec1c0aea4eaa29f89109c4`.
- PASS — the canonical showcase generator/check agrees on 30 frames, 230,904
  bytes, and SHA-256
  `608dec0905e713b0e1343de5259ac4c96b34f296836ce80394bf3f935d5f9fd7`.
- PASS — final serial Chrome `152.0.7977.64` Worker/WebGL2 report (`26,941` bytes,
  SHA-256 `c2fe2379ae2e3175b26be846d808e95e0dde507039209a02c51f8f6825a455ab`),
  fallback/WebGL2 (`26,903` bytes,
  `9bb3e97d2355d2b35b529a36cd2806896167d2091ea8b2de864088952a4a530d`),
  and fallback/Canvas 2D (`26,907` bytes,
  `633ee01595fa6e3368aceb6fe7470ced93ed0d0268deee078297570539f27494`).
- All three browser receipts prove the green root pixel and focus alignment
  `0.9999999953`, one ready root, component/quota/tier/packed-edge semantics,
  exact one-cell purchase and incident frontier changes, far/close and
  center/limb region hierarchy, bounded nine-button DOM, keyboard traversal,
  forced colors, stable reduced motion, eight viewports at 200% text, no
  horizontal overflow, retained layout/substrate through context loss, zero
  steady edge rebuilds, and no browser errors. WebGL remains four draws with
  unchanged `1,838,196` static and `325,152` dynamic bytes; Canvas dynamic bytes
  remain `130,560`.
- Two stable serial p95 entry/snapshot/update/steady samples span Worker/WebGL2
  `14.9–16.6/0.7–1.5/2.0–2.2/1.1–2.3 ms`, fallback/WebGL2
  `14.5–17.7/0.6/1.5–1.6/1.3–1.4 ms`, and fallback/Canvas
  `5.5–6.0/0.5–0.6/1.8–1.9/1.5–1.7 ms`. Baseline values were
  `16.2/0.7/1.5/1.1`, `15.6/0.5/1.4/1.1`, and `7.8/0.7/1.8/2.2 ms`;
  percentage swings do not reproduce consistently and absolute work remains
  bounded.
- PASS — the fresh complete `npm run verify` passed all 26 gates in `326.175 s`,
  including 231 unit tests, 76 integration tests, every production audit,
  generated showcase, agent and balance smoke, terminal soak, benchmark,
  structure, identity, and links. Retained log
  `reports/evolution-ability-regions-v1-verify.log` is 145,944 bytes, SHA-256
  `5002371c83e3ccbdedd1ee372ca2fba2a3360c6eee8942685947c5adc78e073d`.

## Failed, unavailable, and pending evidence

- FAILED, not a pass — the first unconfigured browser command exited `77`
  because Chrome was absent from `PATH`; explicit cached Chrome produced the
  receipts above.
- FAILED, not a pass — the first revised agent smoke loaded an unsorted synthetic
  level vector, so current validation reset it and exposed only the root. Sorting
  the canonical fixture fixed the test input; the production validator was not
  weakened.
- SUPERSEDED, not a pass — the predecessor tournament demanded natural
  specialist ownership within five Worlds. Connected routes need 15–26 hops;
  the retained observation is false for Luminous while direct legal routes and
  campaign progress pass. No economy or ecology value changed to hide it.
- SUPERSEDED, deleted — a local greedy simultaneous-frontier candidate enclosed
  a domain before capacity. The final bounded power-plus-frontier partition is
  the sole production algorithm; no candidate mode remains.
- STALE for timing — one parallel browser batch passed behavior but included
  contention. Only the later serial report hashes above are performance evidence.
- FAILED, not a pass — the first complete `npm run verify` attempt ran all 26
  gates in `318.731 s`; 24 passed, while `audit:trophies` retained predecessor
  scattered-layout horizon bands and `showcase:check` detected generated source
  metadata changed by the catalog tier labels. The Trophy calibration bands now
  describe its production connected-cell route and the canonical showcase was
  regenerated. Retained failed log
  `reports/evolution-ability-regions-v1-verify-failed.log` is 145,793 bytes,
  SHA-256 `206ec8287c359e34076ccc8ceb410214874fb48f5b62b73d59daee6f6033c388`.
- FAILED before verification, not a run or pass — the first timing wrapper exited
  `127` because `/usr/bin/time` is absent. The transfer-note restoration trap ran,
  their hashes stayed exact, and Bash's built-in timer started the actual run.
- PUBLICATION-GATED — local verification is complete. Final diff/accounting and
  the coherent campaign commit must precede publication. Exact remote ref,
  exact-SHA CI/Pages, cache-busted deployed bytes, and deployed Worker/WebGL2,
  fallback/WebGL2, and fallback/Canvas results belong to the final handoff.
  Successful handoff evidence makes this record terminal; any failed or absent
  item reopens this package at that item and is not a pass.
- UNAVAILABLE — physical-device, real screen-reader, high-refresh, thermal, and
  hardware safe-area evidence.

## Exact next coherent step

`none` when the final handoff's exact-revision remote, CI, Pages, deployed-byte,
and three deployed-browser gates pass; otherwise the first failed external gate.
