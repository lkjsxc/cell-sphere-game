# tests/unit/

Environment-independent production-module tests (`node --test`). Coverage
includes deterministic primitives, scene/phase transitions, shell semantics,
settings and persistence migration, geodesic topology, whole-cell geography,
renderer geometry, local-resource conservation, event eras, ecological/habitat
access, monotone SCORE v3, crash-recoverable result transactions, the 252-cell
Evolution/build graph, and current/legacy Trophy proof isolation.
